kind: Namespace
apiVersion: v1
metadata:
  name: external-secrets
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: demo-secret
  namespace: external-secrets
spec:
  refreshInterval: "1m"
  secretStoreRef:
    name: openbao-store
    kind: SecretStore
  target:
    name: my-demo-secret
  data:
    - secretKey: password
      remoteRef:
        key: secret/data/demo
        property: password
---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: openbao-store
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  project: homelab
  destination:
    namespace: external-secrets
  provider:
    vault:
      server: "http://openbao.openbao.svc.cluster.local:8080"
      path: "kubernetes"
      auth:
        kubernetes:
          mountPath: "kubernetes"  # o caminho onde habilitamos o auth
          role: "external-secrets"
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-secrets
  namespace: argocd
spec:
  destination:
    namespace: external-secrets
    server: https://kubernetes.default.svc
  project: homelab
  source:
    chart: external-secrets
    repoURL: https://charts.external-secrets.io
    targetRevision: 0.20.3
  syncPolicy:
    syncOptions:
      - ServerSideApply=true
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true